<?php $isLogin = $this->helper('customer')->isLoggedIn()?>
<?php if(!$isLogin){?>
<?php $this->setBackUrl()?>
<button id="bt-loginlinked-popup" class="bt-login-social" alt="<?php echo $this->__('Login by Linkedin') ?>" title="<?php echo $this->__('Login by Linkedin') ?>" onclick="linkedLogin()" href="javascript:void(0);">
	<span><span><?php echo $this->__('Linkedin Sign in') ?></span></span>
</button>
<script type="text/javascript">
var newwindow;
var intId;
function linkedLogin(){
	var  screenX    = typeof window.screenX != 'undefined' ? window.screenX : window.screenLeft;
	var	 screenY    = typeof window.screenY != 'undefined' ? window.screenY : window.screenTop;
	var	 outerWidth = typeof window.outerWidth != 'undefined' ? window.outerWidth : document.body.clientWidth;
	var	 outerHeight = typeof window.outerHeight != 'undefined' ? window.outerHeight : (document.body.clientHeight - 22);
	var	 width    = 790;
	var	 height   = 350;
	var	 left     = parseInt(screenX + ((outerWidth - width) / 2), 10);
	var	 top      = parseInt(screenY + ((outerHeight - height) / 2.5), 10);
	var	 features = (
			'width=' + width +
			',height=' + height +
			',left=' + left +
			',top=' + top
		  );

	newwindow=window.open('<?php echo $this->getLoginUrl() ?>','Login_by_Linkedin',features);

	if (window.focus) {
		newwindow.focus()
	}
	return false;
}
function opensocialLogin(){
	javascript:socialLogin.open();
}
</script>
<?php }?>